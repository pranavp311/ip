package kopi;

import javafx.application.Application;

/** Launches the JavaFX application from a non-Application class. */
public class Launcher {
    /** Starts the Kopi GUI. */
    public static void main(String[] args) {
        Application.launch(Main.class, args);
    }
}
